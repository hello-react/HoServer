import _ from 'lodash'
import React from 'react'

/**
 * plugin for third part login
 */
class SocialLogin {
    constructor() {
        // _.bindAll(this, ['init', 'renderImportExportMenu'])
    }

    init(pluginManager) {
        //
    }
}

export default new SocialLogin()
