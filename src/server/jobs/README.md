we use node-schedule(#https://github.com/node-schedule/node-schedule) to manage jobs, 
to enable jobs:
- modify config, set server.enableJob=true
- put jobs script to this directory, and restart service
